# Polymarket & APIs
py-clob-client>=0.19.0
requests>=2.28.0

# Backend
python-dotenv>=1.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Optional: persist total P&L / state
# (use JSON file or SQLite; no extra deps for JSON)
